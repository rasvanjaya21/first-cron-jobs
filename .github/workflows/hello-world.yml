name: Hello World Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello World
        run: echo "Hello World"